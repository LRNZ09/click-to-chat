import 'package:flutter/material.dart';

import 'app.dart';

void main() {
  runApp(App());

  // TODO: enable smooth scroll on high refresh rate screens
  // GestureBinding.instance?.resamplingEnabled = true;

  // TODO: keep screen on in debug mode
  // Wakelock.toggle(on: isInDebugMode);
}
